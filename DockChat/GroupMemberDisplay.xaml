<UserControl
    x:Class="DockChat.GroupMemberDisplay"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DockChat"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="100"
    d:DesignWidth="75"
    x:Name="MemberDisplayUserControl">
    
    <Grid>
        <Grid VerticalAlignment="Top" Height="75" Width="75">
            <Image Height="60" Width="60" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Source="{Binding ElementName=MemberDisplayUserControl, Path=ImageUrl}"></Image>
        </Grid>
        <TextBlock HorizontalAlignment="Center" Margin="0,75,0,0" Text="{Binding ElementName=MemberDisplayUserControl, Path=MemberName}"></TextBlock>
    </Grid>
</UserControl>
